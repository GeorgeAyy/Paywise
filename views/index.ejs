<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Welcome to the Application</title>
    <!-- Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css"
      integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm"
      crossorigin="anonymous"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <!-- Custom CSS -->
    <style>
      body {
        padding-top: 5rem;
      }
    </style>
  </head>
  <body>
    <%- include('partials/nav') %>
    <div class="container">
      <div class="row mt-5">
        <div class="col-md-6 offset-md-3">
          <div class="card text-center">
            <div class="card-header">Welcome to the Application</div>
            <div class="card-body">
              <h5 class="card-title">Choose an option:</h5>
              <% if (!user) { %>
              <a href="/auth/login" class="btn btn-primary">
                <i class="bi bi-box-arrow-in-right"></i> Login
              </a>
              <a href="/auth/signup" class="btn btn-success">
                <i class="bi bi-person-plus-fill"></i> Signup
              </a>
              <% } else { %>
              <a href="/user/display" class="btn btn-info">
                <i class="bi bi-people"></i> Display All Users
              </a>
              <a href="/user/profile" class="btn btn-secondary">
                <i class="bi bi-person"></i> Profile
              </a>
              <a href="/auth/logout" class="btn btn-danger">
                <i class="bi bi-box-arrow-right"></i> Logout
              </a>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Bootstrap JS Bundle (Popper.js included) -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-gGpQyYoNzO3vMN0DyUbyjkp+hB1hjZA9JcP9qUp7k3DHgosqJqRU5+jUEmhS8u5f"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
